def bin2dec(binario):
    dec=int(str(binario), 2)
    return dec
def dec2bin(decimal):
    bina= ""
    bin(dec)
    bina= int(bin(numero)[2:])
    return bina

def errorOperacion(opcionError=0):
	regresar = input("¿Quiere realizar una nueva operanción [S/N]? ")
	return (regresar)

dec=numerobinario = 0
    numerodecimal = 0
    numero=int(input("dame un numero entero: "))
    numerodecimal= dec2bin(dec)
    numerobinario= bin2dec(numerodecimal)
    print("numerobinario {}" .format(numerobinario))
    print("numerodecimal {}" .format(numerodecimal))

  
		
